// PulseWake — single source of truth for App Store / TestFlight versions.
// Product → Archive runs scripts/bump_build_number.sh (patch + build +1).

MARKETING_VERSION = 1.0.5
CURRENT_PROJECT_VERSION = 6
